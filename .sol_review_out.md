CRITICAL — adapters/oauth-sync-macos/src/lib.rs:107 — `FirstRecord` is based on a stale read and is not compare-and-set, so the fence does not guarantee cross-account isolation without the deferred whole-cycle permit — empty store → A and B both load `None` → A records A and commits fenced A mail → B resumes its stale `RecordOnly`, overwrites the identity with B without clearing A’s mail → A’s mail remains after ownership moves to B and may coexist with B’s mail — make identity reconciliation atomically validate the observed prior state under `BEGIN IMMEDIATE`; an absent-row decision must insert only if the row is still absent, otherwise abort/retry and recompute the clear-or-preserve action, with a deterministic two-gate interleaving regression test.

MINOR — adapters/oauth-sync-macos/src/lib.rs:690 — the gate-to-sync test fake ignores the received fence, so the claimed handoff is not behaviorally tested — fresh hash `[5; 32]` → `gated_sync` accidentally passes a constant or unrelated hash → `a_passing_gate_runs_the_bounded_sync` still passes because it only counts reconciles, while the real store incorrectly aborts or accepts writes — record the fence received by both fake write methods and assert it equals the hasher output for match, first-record, and identity-change paths.